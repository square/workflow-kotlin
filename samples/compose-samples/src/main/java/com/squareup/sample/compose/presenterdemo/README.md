# presenterdemo

This package contains a self-contained demo prototype of the capabilities of the `workflow-compose`
library prototype.i

- The `structurednav` package contains an opinionated library for expressing navigation.
- The `app` package contains code that would live in the app project for a simple demo app. 
